name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  tests:
    name: Pure-logic tests
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: swift test
        run: swift test
